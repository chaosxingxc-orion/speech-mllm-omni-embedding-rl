# `minds14_v2_multiplicity.json` — superseded note

**2026-07-12 (RI mechanical remediation item 4; forensic finding 续15: "MInDS '7/7' 实为 5 独特对比").**

This file is kept **append-only** (never edited/deleted — CLAUDE.md research-integrity discipline)
as the original, now-superseded record. Do not cite its "7/7 deltas survive Holm" headline going
forward.

**What was wrong:** this file's own `note_on_duplicates` field already documented that 2 of its 7
named comparisons are numerically IDENTICAL duplicates of 2 other comparisons in the same file —
`factor_isolation_deltas.instruction_effect__cards_absent` == `deltas_vs_naive.instruction_only`,
and `factor_isolation_deltas.cards_effect__instruction_absent` == `deltas_vs_naive.cards_only` (both
pairs computed by the exact same `paired_bootstrap(...)` call in
`scripts/repro_minds14_toolintent_v2.py`, just reported under two different keys). Running a
Holm/max-T family-wise correction over a 7-row family that silently double-counts 2 hypotheses is
not the correct correction — the true family is **5 unique pairwise contrasts**.

Also: no script that generated this file was ever committed (`git log --all --diff-filter=A
--name-only` shows it landing in commit `f154886` with no corresponding generator) — a second,
independent finding of the same forensic review.

**Superseding artifact:** `_repro/minds14_v2_multiplicity_v2.json`, generated by the newly committed
`scripts/minds14_multiplicity_v2.py`, which recomputes the same bootstrap/Holm/max-T machinery
(W1's `scripts/baselines/stats.py`) over the 5 deduped contrasts only.

**Result:** the conclusion does not flip — all 5 unique contrasts survive Holm at alpha=0.05 (max-T
agrees, 5/5) — but it is now stated over the correct, non-inflated family size instead of the
double-counted 7.

See `docs/claim_ledger.yaml` entry `C-MINDS-V2` (`hardening_update_2026_07_11` field) for the
ledger-authoritative reconciliation.
